# Fixtures Data

This directory contains frozen datasets for integration testing.

## Policy
1. Do not manually edit large data files.
2. Use `scripts/data_freeze.sh` to update data.
3. Check MD5 sums before running tests.

## Files
*No fixtures frozen yet.*
