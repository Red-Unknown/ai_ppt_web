## 5. 代码风格规范 (Code Style)

### 5.1 Python 风格

* **标准**：遵循 **PEP 8**。
* **强制工具**：`Black` (格式化), `isort` (导入排序), `Flake8` (静态检查)。
* **类型提示**：关键业务逻辑必须包含 Type Hints。

### 5.2 错误处理

* **禁止裸露的 except**：严禁使用 `except:`。
* **日志**：异常必须记录日志（含堆栈）。