[pytest]
pythonpath = ../..
testpaths = backend/tests
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
